e1 = pair zero (pair zero (pair zero nil))
e2 = pair zero (pair one (pair one nil))
e3 = pair one (pair zero (pair one nil))
e4 = pair one (pair one (pair zero nil))

dfa = pair e1 (pair e2 (pair e3 (pair e4 nil)))
input = pair one (pair zero (pair one (pair one (pair zero))))
